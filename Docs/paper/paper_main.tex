% =====================================================================
% Vmem-phi -- MASTER manuscript file (Overleaf-ready).
%
% HOW TO USE ON OVERLEAF
%   Upload the whole Docs/ folder (or a zip of it) and set this file as the
%   main document. It \inputs:
%     * paper_sections_theory_hardware.tex   (corruption suite + theory + HW; already drafted)
%     * paper_sections/*.tex                 (intro, related work, motivation expt, qualitative,
%                                              discussion; written for the advisor's review points)
%
% Venue/template: plain `article` for now -- the advisor said the template is not
% the priority. Swap \documentclass for IEEEtran / cvpr / neurips when the target
% venue is fixed (the section files are template-agnostic).
%
% Measured numbers live in Docs/paper_figures.md (do NOT hard-code results without
% a pointer back to source). Result tables that depend on a GPU run are marked
% with `% TODO(advisor)` / `\todofig` placeholders.
% =====================================================================
\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

% --- advisor review-point markers (render in the margin; comment out \showtodos to hide) ---
\newcommand{\showtodos}{}
\newcommand{\advisor}[1]{\ifdefined\showtodos{\textcolor{blue}{\small[\textbf{advisor:} #1]}}\fi}
\newcommand{\todofig}[1]{\fbox{\parbox{0.9\linewidth}{\centering\textcolor{red}{\textbf{FIGURE/TABLE TODO:} #1}}}}

% Tell the embedded theory/hardware file to skip its standalone wrapper + bib;
% this master supplies the preamble, \begin{document}, and the bibliography.
\newcommand{\theoryhardwareEMBED}{}

\title{Vmem-$\varphi$: Zero-Cost Out-of-Distribution Detection in Spiking\\
Neural Networks from Membrane-Potential Statistics}
\author{(authors)}
\date{}

\begin{document}
\maketitle

\input{paper_sections/abstract}

\input{paper_sections/intro}

\input{paper_sections/related_work}

% ---- Corruption suite + algorithms + leaky-integrator theory + neuromorphic HW ----
% (already drafted and compilable; embedded via \theoryhardwareEMBED above)
\input{paper_sections_theory_hardware}

% ---- Motivation: a SOTA detector collapses under these corruptions (advisor point 2) ----
\input{paper_sections/motivation_experiment}

% ---- Method (MDD) + main OOD results: pull numbers from Docs/paper_figures.md ----
\input{paper_sections/results}

% ---- Qualitative analysis (advisor's "we also need a thorough qualitative analysis") ----
\input{paper_sections/qualitative}

% ---- Discussion: physical-cause mapping (pt 3) + neuroscience of floods (pt 4) ----
\input{paper_sections/discussion_bio_physical}

% ---- Limitations + conclusion ----
\input{paper_sections/limitations_conclusion}

\input{paper_sections/bibliography}

\end{document}
