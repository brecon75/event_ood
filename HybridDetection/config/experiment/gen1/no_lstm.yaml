# @package _global_
defaults:
  - default

model:
  backbone:
    name: v1_attention_no_lstm
    embed_dim: 48
    stage:
      attention:
        dim_head: 24
  fpn:
    depth: 0.33

dataset:
  sequence_length: 1
