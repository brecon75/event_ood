[pytest]
testpaths = tests
addopts = -q -p no:cacheprovider
markers =
    slow: end-to-end tests that load the real model/checkpoint (skipped by default)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
